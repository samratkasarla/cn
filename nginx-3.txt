## Nginx have ability to handle huge amount of concurrent requests.

## C10k problem

## There are mainly four types of web servers - 1.Apache 2.IIS 3.Nginx 4.LiteSpeed 5.apache tomcat

## Apache have modular structure.

## Nginx comprises of IMAP/POP3 proxy server. High performance, stability, simple configuration and low resource usage.

## What are threads ?

## How to check whether we have sudo priviledges ??

## how to check private Ip on linux ??

## inheritance in directives and programming languages


sudo apt update && sudo apt upgrade -y
sudo apt install nginx -y
sudo systemctl status nginx

80

cd /etc/nginx
ls -lh
cat nginx.conf
sudo mv nginx.conf nginx.conf.backup
sudo touch nginx.conf
sudo vim nginx.conf
sudo nginx -t

sudo systemctl restart nginx
sudo nginx -s reload
curl -i http://nginx-handbook.test

error: curl: (6) Could not resolve host: nginx-handbook.test
172-26-2-244
sudo vim /etc/hosts
=============================================================
curl -I http://nginx-handbook.test/mini.min.css

** I am able to print simple website which can show simple msg with nginx.

** Getting the response from different ports of same server.
** Getting response from same diffsrver_name but from same port in same server.

 library.test
librarian.library.test

** prefix match and regex match
** regex match has more priority than a prefix match.

** Preferential prefix match-----including ^~ after location context.

** So descending order of the priority is Exact (=), Preferencial_prefix (^~),REGEX (~ or ~*), Prefix (none).
ls

** variables in Nginx---String, Integer, Boolean
** nginx as a reverse_proxy...

** Variables in linux
** Redirects and Rewrites---Now if we send request 54.204.162.162/about_page it automatically changes to 54.201.162.162/about.html.
-----in Rewrites the url remains unchanged.

** try_files
** Logging in nginx.
** Access_log directive and error_log directive.
** Eight levels of error messages ---- debug...info...notice...warn...error...crit...alert...emerge
** 








